This is an image encoder of shufflenet v2 based on paddlehub.

Command for building docker:

```
sudo docker build -t shufflenetv2 ./
```

Command for running the docker:

```
sudo docker run  --network=host shufflenetv2
```