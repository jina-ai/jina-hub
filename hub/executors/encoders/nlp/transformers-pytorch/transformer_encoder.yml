!TransformerTorchEncoder
with:
  model_name: bert-base-uncased

requests:
  on:
    [SearchRequest, IndexRequest]:
      - !EncodeDriver
        with:
          method: encode

    ControlRequest:
      - !ControlReqDriver {}