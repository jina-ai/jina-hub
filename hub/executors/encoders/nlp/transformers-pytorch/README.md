# Demo Transformer Encoder

Just an example to show how simple an executor could be. It pre-downloaded the `bert-base-uncased` model in the image, so you don't need to download it again when using the image. 