!AnnoyIndexer
with:
  ref_indexer:
    !NumpyIndexer
    metas:
      name: wrap-npidx
      workspace: $TEST_WORKSPACE
    with:
      backend: numpy
      index_filename: wrap-npidx
      metric: euclidean
metas:
  workspace: $TEST_WORKSPACE
